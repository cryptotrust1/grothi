'use client';

// Client-side scheduler interactions (for future drag-and-drop, etc.)
export function SchedulerClient({ botId }: { botId: string }) {
  return null; // Placeholder for future client-side calendar interactions
}
